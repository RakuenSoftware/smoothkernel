# Compatibility copy of the shared Smooth* sample env.
# Prefer examples/smooth.env for new setups.

KERNEL_VERSION=6.19.12
LOCALVERSION=-smooth
ZFS_VERSION=2.4.1

# Canonical config checked into the repo.
CONFIG_SOURCE=$(CURDIR)/configs/smooth-amd64.config

# Ordered patch lanes. Override only if you are testing a different lane.
CACHYOS_PATCHSET=cachyos-6.19.12
NOBARA_PATCHSET=nobara-picks
POST_NOBARA_PATCHSET=post-nobara-6.19.12

# Where the .debs land. Default is $(pwd)/out.
OUT_DIR=$(CURDIR)/out

# Build parallelism. Default is $(nproc).
BUILD_THREADS=14

# Strip BTF/DWARF debug info to slim the kernel image and speed up
# packaging. Set to 0 if you want full debug info.
STRIP_DEBUG_INFO=1

# NAS / server network-path tuning. Enables BBR + FQ as built-in, flips
# the default TCP congestion control to BBR, enables NET_RX_BUSY_POLL +
# TCP_MD5SIG + TCP_CONG_DCTCP. Paired with /etc/sysctl.d drop-ins in the
# consuming image. Set to 0 to build a reference kernel that matches
# upstream defaults.
NET_TUNING=1

# Server / appliance general tuning. Enables PREEMPT_DYNAMIC,
# RCU_NOCB_CPU, and BLK_WBT_MQ. None of these change behaviour without
# corresponding cmdline / sysctl setup, so compiling them in is cheap.
SERVER_TUNING=1

# Trim obviously irrelevant driver families for the headless appliance line.
APPLIANCE_TRIM=1
